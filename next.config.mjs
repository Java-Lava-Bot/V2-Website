/** @type {import('next').NextConfig} */
const nextConfig = {
  // Produce a minimal, self-contained server build for deployment
  output: 'standalone',
  reactStrictMode: true,
};

export default nextConfig;
